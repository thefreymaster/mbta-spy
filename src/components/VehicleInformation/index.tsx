import { Dialog } from "@mantine/core";

export const VehicleInformationDialog = () => {
  return (
    <Dialog
      position={{ bottom: 20, left: 20 }}
      opened
      onClose={() => {}}
      size="lg"
      radius="md"
    >
      Test
    </Dialog>
  );
};
